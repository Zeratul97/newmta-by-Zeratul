<meta>
    <info type="script" name="nwmta_dev" />

    <!-- Fájlok -->
    <script src="global.lua" type="shared" cache="false" /> <!-- A cache opció tiltja hogy a játékosoknak letöltődjön az adott fájl(védelem miatt) -->
    <script src="server.lua" type="server" />
    <script src="client.lua" type="client" cache="false" /> <!-- A cache opció tiltja hogy a játékosoknak letöltődjön az adott fájl(védelem miatt) -->

    <!-- Export funkciók -->
    <export function="getDevSerials" type="shared" />
    <export function="getCMDDisabled" type="server" />
    <export function="getDeveloperCMD" type="server" />

    <!-- Alap kliens és szerver verzió(néhány funkció igényli!!!!) -->
    <min_mta_version server="1.5.6" client="1.5.6" />
</meta>
